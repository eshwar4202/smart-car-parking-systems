import { StyleSheet, View, Image, Button, TouchableOpacity } from "react-native";

export default function Usrav() {
  return (
    <View style={styles.circle}>

    </View>
  );


}

const styles = StyleSheet.create({
  circle: {
    backgroundColor: 'red',
    width: 40,
    height: 40,
    borderRadius: 25,
  }
})
